/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.sanity.io',"cloud.appwrite.io", 'images.pexels.com','media.istockphoto.com'],
    },
  };
  
  module.exports = nextConfig;